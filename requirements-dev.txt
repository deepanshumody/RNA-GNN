# Development tooling (linting + tests). Install with:
#   pip install -r requirements-dev.txt
pytest
ruff

# Test-runtime deps for the lightweight (non-deep-learning) tests. The torch /
# torch-geometric / rdkit tests skip automatically when those aren't installed.
numpy
scikit-learn
